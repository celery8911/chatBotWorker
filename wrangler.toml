name = "chatbot-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# 开发环境变量在这里设置（非敏感信息）

# 注意：敏感信息（如 OPENAI_API_KEY）需要通过命令行设置
# 本地开发: 在 .dev.vars 文件中设置
# 生产环境: 运行 wrangler secret put OPENAI_API_KEY

[[routes]]
pattern = "api.qincai.digital"
custom_domain = true
